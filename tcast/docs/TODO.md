# tcast docs backlog

## Todo

- [ ] Command reference: switch / restore / status / clean / top / menu
- [ ] `/var/lib/tcast` layout (`git.map`, conf)
- [ ] `programs.tcast` NixOS module
- [ ] Relationship to NDS seed vs module install
