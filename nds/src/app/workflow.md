# Main
 - pipefail
 - bash compatibility (configure bash version)
 - get variables for script dir, app dir, version ect
 - hooks, traps etc
 - elevateToRoot
 - setup runtime dir
 - cleanup runtime dir
 - load logger
 - start logger

 what needs hooks:
  - action custom hooks
  - cleanup log
  - bundle creator
  - show logs on error


 x action gathering
 x select action
 x warmup gh
 x load settingsManager
 x load features
 x load utilities


## base
